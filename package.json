{
  "name": "universal-agent-skills",
  "version": "1.0.0",
  "private": true,
  "description": "Composable engineering, frontend design, and cross-session continuity skills for agent harnesses",
  "repository": {
    "type": "git",
    "url": "https://github.com/kaizen2026/universal-agent-skills"
  },
  "license": "MIT",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-plugin-version.mjs && node scripts/sync-runtime.mjs && node scripts/sync-exchange.mjs",
    "check-plugin-version": "node scripts/sync-plugin-version.mjs --check",
    "sync-runtime": "node scripts/sync-runtime.mjs",
    "check-runtime": "node scripts/sync-runtime.mjs --check",
    "sync-exchange": "node scripts/sync-exchange.mjs",
    "check-exchange": "node scripts/sync-exchange.mjs --check",
    "validate:skills": "node scripts/validate-skills.mjs",
    "test:runtime": "node --test tests/*.test.mjs",
    "test:installer": "node scripts/installer-smoke.mjs",
    "test:plugin": "node scripts/claude-plugin-smoke.mjs",
    "test": "npm run check-plugin-version && npm run check-runtime && npm run check-exchange && npm run validate:skills && npm run test:runtime"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.30.0",
    "yaml": "^2.9.0"
  },
  "packageManager": "npm@10.9.4"
}
