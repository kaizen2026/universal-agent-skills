# Local Markdown issue tracker seed

Record `.scratch/<feature>/issues/` as the issue location, one Markdown file per issue, stable ids, status fields, and textual blocking edges. Keep `.scratch/` ignored unless the user chooses to track it.
