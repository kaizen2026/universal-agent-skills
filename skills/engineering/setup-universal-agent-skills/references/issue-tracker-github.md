# GitHub issue tracker seed

Record the repository, CLI command patterns for reading/creating/commenting/closing issues, native blocking operations, and the fact that pull requests are excluded from triage unless the user enables them.
