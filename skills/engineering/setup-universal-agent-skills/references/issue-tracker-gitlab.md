# GitLab issue tracker seed

Record the project, `glab` command patterns for reading/creating/commenting/closing issues, supported blocking operations, and the fact that merge requests are excluded from triage unless the user enables them.
